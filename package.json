{
  "name": "ng_2502-workspace",
  "private": true,
  "scripts": {
    "start:emulator": "firebase emulators:start",
    "start:backend:local": "cross-env ENV=local PYTHONIOENCODING=utf-8 functions\\venv\\Scripts\\python -m uvicorn functions.main:app --host 0.0.0.0 --port 8000 --reload",
    "start:backend:dev": "cross-env ENV=dev PYTHONIOENCODING=utf-8 functions\\venv\\Scripts\\python -m uvicorn functions.main:app --host 0.0.0.0 --port 8000 --reload",
    "start:frontend": "cd mobile && npx expo start",
    "dev": "npm run dev:local",
    "dev:local": "concurrently -k -c \"blue,green\" -n \"EMULATOR,BACKEND\" \"npm run start:emulator\" \"npm run start:backend:local\"",
    "dev:remote-db": "npm run start:backend:dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0"
  }
}
